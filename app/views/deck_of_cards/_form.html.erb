<%= form_for(@deck_of_card) do |f| %>
  <% if @deck_of_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deck_of_card.errors.count, "error") %> prohibited this deck_of_card from being saved:</h2>

      <ul>
      <% @deck_of_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
